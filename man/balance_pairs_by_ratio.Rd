% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transcript_matching_functions.R
\name{balance_pairs_by_ratio}
\alias{balance_pairs_by_ratio}
\title{Balance matched positive/negative pairs by m6A ratio bins}
\usage{
balance_pairs_by_ratio(
  gr,
  ratio_col = "Ratio",
  label_col = "label",
  matched_negative_id_col = "matched_negative_id",
  matched_positive_id_col = "matched_positive_id",
  split_col = NULL,
  balance_splits = "training",
  breaks = seq(0, 1, by = 0.2),
  bin_width = NULL,
  clamp_ratio = TRUE,
  missing_ratio = c("drop", "keep"),
  target_n_per_bin = NULL,
  strict_reciprocal = TRUE,
  drop_conflicts = TRUE,
  require_same_split = TRUE,
  seed = 1L,
  return_diagnostics = TRUE
)
}
\arguments{
\item{gr}{A \code{GRanges} containing positives and negatives, plus match columns.}

\item{ratio_col}{Metadata column containing the ratio in \link{0,1} (e.g. "Ratio").}

\item{label_col}{Metadata column containing 0/1 labels (default "label").}

\item{matched_negative_id_col}{Column giving the matched negative ID for each positive.}

\item{matched_positive_id_col}{Column giving the matched positive ID for each matched negative.
Only required if \code{strict_reciprocal=TRUE}.}

\item{split_col}{Optional split column (e.g. "dataset_split"). If provided, only rows
in \code{balance_splits} are resampled; other splits are kept unchanged.}

\item{balance_splits}{Which split label(s) to balance (default "training").}

\item{breaks}{Optional numeric breaks for binning the ratio. Default makes 0.2-width bins.}

\item{bin_width}{Optional alternative to \code{breaks}. If provided, builds breaks as
\code{seq(0, 1, by=bin_width)} (ensuring 1 is included).}

\item{clamp_ratio}{If TRUE, clamp ratio values to \link{0,1} before binning.}

\item{missing_ratio}{What to do with positives that have missing/non-finite ratio:
"drop" removes them from the balanced subset; "keep" keeps them (unbalanced).}

\item{target_n_per_bin}{Optional integer. If NULL (default), uses the minimum count
among non-empty bins (pure downsampling, perfectly balanced across non-empty bins).
If provided and larger than that minimum, it is reduced to the minimum (no upsampling).}

\item{strict_reciprocal}{If TRUE, require the negative to point back to the same positive.}

\item{drop_conflicts}{If TRUE, enforce that a negative is used at most once (drop duplicates).}

\item{require_same_split}{If TRUE and \code{split_col} is provided, require that each kept
positive/negative pair lies in the same split label.}

\item{seed}{Random seed for reproducible downsampling.}

\item{return_diagnostics}{If TRUE, attach a \code{"balance_ratio_diagnostics"} attribute.}
}
\value{
A subset \code{GRanges} with balanced positives (in the chosen split(s))
plus their matched negatives, and all untouched rows from other splits (if any).
}
\description{
Downsamples \emph{positive} sites (label==1) to equalize their distribution across
user-defined ratio bins (e.g. 0-0.2, 0.2-0.4, ...), while preserving matched pairs:
for every retained positive, its matched negative is kept as well.
}
\details{
By default, this is intended to be applied to the \emph{training} split only,
leaving validation/testing untouched to preserve realistic evaluation.
}
