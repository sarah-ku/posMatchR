% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transcript_matching_functions.R
\name{subset_matched_pairs}
\alias{subset_matched_pairs}
\title{Subset a GRanges to balanced matched positive/negative pairs}
\usage{
subset_matched_pairs(
  gr,
  matched_negative_id_col = "matched_negative_id",
  matched_positive_id_col = "matched_positive_id",
  is_positive_col = NULL,
  label_col = "label",
  strict_reciprocal = TRUE,
  drop_conflicts = TRUE,
  return_diagnostics = TRUE
)
}
\arguments{
\item{gr}{A \code{GRanges} with canonical match columns produced by
\code{\link{match_background}} or \code{\link{random_drach_within_transcript}}.}

\item{matched_negative_id_col}{Column containing the matched negative ID for each positive.}

\item{matched_positive_id_col}{Column containing the matched positive ID for each negative.}

\item{is_positive_col}{Optional column indicating positives (1/0). If NULL, uses \code{label_col}.}

\item{label_col}{Label column (0/1) used when \code{is_positive_col} is NULL.}

\item{strict_reciprocal}{If TRUE enforce reciprocal links (negative points back to the same positive).}

\item{drop_conflicts}{If TRUE drop duplicated negative assignments if present.}

\item{return_diagnostics}{If TRUE attach a \code{"subset_pairs_diagnostics"} attribute.}
}
\value{
A \code{GRanges} containing only matched pairs (2 rows per pair).
}
\description{
Extracts a strictly paired dataset from canonical match columns
(\code{matched_negative_id}, \code{matched_positive_id}) so the output contains
exactly one negative per positive for all retained pairs.
}
