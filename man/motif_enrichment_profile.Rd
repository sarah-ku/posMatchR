% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08-motif-profile.R
\name{motif_enrichment_profile}
\alias{motif_enrichment_profile}
\title{Compute a motif-enrichment profile around single-nucleotide sites}
\usage{
motif_enrichment_profile(
  gr,
  genome,
  motif,
  window = 250L,
  set_col = NULL,
  positive_value = "positive",
  negative_value = "matched_negative",
  include_other = FALSE,
  matched_only = FALSE,
  method = c("auto", "iupac", "universalmotif"),
  motif_name = NULL,
  scan_rc = FALSE,
  hit_position = c("center", "start", "end"),
  threshold = 0.8,
  threshold_type = "logodds",
  nthreads = 1L,
  bin_size = 1L,
  smooth_window = 1L,
  seqstyle = NULL,
  chrs = NULL,
  drop_unstranded = TRUE,
  drop_edge_positions = FALSE,
  edge_trim = NULL,
  center_exclude = 0L,
  window_mode = c("auto", "transcript", "genomic"),
  txdb = NULL,
  resources = NULL,
  tx_name_col = "tx_name",
  tx_pos_col = "tx_pos"
)
}
\arguments{
\item{gr}{A single-nucleotide \code{GRanges}. For matched-set comparisons, pass
\code{subset_matched_pairs(gr)} or use \code{matched_only = TRUE}.}

\item{genome}{A BSgenome-like object accepted by \code{Biostrings::getSeq()}.}

\item{motif}{A character vector of exact/IUPAC motifs, or a universalmotif object/list.}

\item{window}{Number of bp/nt on each side of the site.}

\item{set_col}{Optional grouping column. Defaults to \code{"match_set"} if present.}

\item{positive_value}{Value in \code{set_col} marking matched positives.}

\item{negative_value}{Value in \code{set_col} marking matched negatives.}

\item{include_other}{Include other groups from \code{set_col}.}

\item{matched_only}{If TRUE and canonical match columns are present, subset to
reciprocal matched pairs before scanning.}

\item{method}{\code{"auto"}, \code{"iupac"}, or \code{"universalmotif"}.}

\item{motif_name}{Optional display name for a supplied motif.}

\item{scan_rc}{Scan reverse-complement motif occurrences as well. Usually FALSE
for strand-oriented transcript/RNA motifs.}

\item{hit_position}{Whether each motif hit is represented by its start, end, or centre.}

\item{threshold}{Threshold passed to \code{universalmotif::scan_sequences()}.}

\item{threshold_type}{Threshold type passed to \code{universalmotif::scan_sequences()}.}

\item{nthreads}{Number of threads for universalmotif scanning.}

\item{bin_size}{Position bin size in bp/nt.}

\item{smooth_window}{Optional moving-average smoothing width in bins. Use 1 for no smoothing.}

\item{seqstyle}{Optional seqlevel style passed to the internal sequence-extraction step.}

\item{chrs}{Optional seqlevels to keep for sequence extraction.}

\item{drop_unstranded}{If TRUE, omit sites on '*' strand because RNA-oriented
motif profiles require a transcript strand.}

\item{drop_edge_positions}{Logical or integer. If TRUE, omit edge positions
from the profile; if FALSE, keep them. For backward compatibility, an integer
value is interpreted as the number of bp/nt to trim from each edge.}

\item{edge_trim}{Integer number of bp/nt to omit from each edge of the profile.}

\item{center_exclude}{Integer number of bp/nt around the central site to omit
from the profile.}

\item{window_mode}{\code{"auto"}, \code{"transcript"}, or \code{"genomic"}.
Use \code{"transcript"} for spliced RNA windows around annotated sites.}

\item{txdb}{Optional \code{TxDb}; used to build transcript resources if
\code{resources} is not supplied.}

\item{resources}{Optional \code{build_tx_resources(txdb)} output. Supplying this
is recommended for repeated motif profiles.}

\item{tx_name_col}{Metadata column containing transcript names.}

\item{tx_pos_col}{Metadata column containing one-based transcript coordinates.}
}
\value{
A data.frame with relative position, group, motif, counts, site counts,
and hits per site.
}
\description{
Counts motif hits in strand-oriented windows centred on each site and returns
a per-position profile. By default, if transcript resources are supplied, the
function extracts spliced transcript/RNA windows using \code{tx_name} and
\code{tx_pos}; otherwise it falls back to contiguous genomic windows with a
warning. Character motifs are treated as exact/IUPAC DNA or RNA patterns; RNA
U is converted to DNA T. Objects from the universalmotif package are scanned
with \code{universalmotif::scan_sequences()}.
}
