% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transcript_matching_functions.R
\name{annotate_sites}
\alias{annotate_sites}
\title{Annotate candidate sites with transcript context and region geometry}
\usage{
annotate_sites(
  gr,
  txdb,
  label_col = "label",
  tx_select = c("longest", "first"),
  seqstyle = NULL,
  chrs = NULL,
  resources = NULL,
  drop_unannotated = FALSE,
  orgdb = NULL,
  gene_keytype = NULL,
  gene_symbol_col = "SYMBOL",
  gene_name_col = NULL,
  cache = NULL
)
}
\arguments{
\item{gr}{A \code{GRanges} of single-nucleotide sites. Must contain \code{label_col}.}

\item{txdb}{A \code{TxDb} object.}

\item{label_col}{Metadata column in \code{gr} containing 0/1 labels.}

\item{tx_select}{Which transcript to select if multiple overlap a site.
\code{"longest"} prefers the transcript with the largest \code{tx_len}.}

\item{seqstyle}{Optional seqlevel style (e.g. \code{"UCSC"}).}

\item{chrs}{Optional character vector of chromosomes/seqlevels to keep.}

\item{resources}{Optional output of \code{\link{build_tx_resources}} for reuse/caching.}

\item{drop_unannotated}{Logical; if TRUE drop rows with \code{location == "unannotated"}.}

\item{orgdb}{Optional \code{OrgDb} for gene ID mapping (e.g. \code{org.Hs.eg.db}).}

\item{gene_keytype}{Keytype used by \code{orgdb} for \code{gene_id} (e.g. \code{"ENTREZID"}).}

\item{gene_symbol_col}{Column to retrieve from \code{orgdb} for gene symbols (default \code{"SYMBOL"}).}

\item{gene_name_col}{Optional column to retrieve for gene names/descriptions (e.g. \code{"GENENAME"}).}

\item{cache}{Optional cache passed to \code{VariantAnnotation::locateVariants}.}
}
\value{
A \code{GRanges} with added metadata including \code{location}, \code{feature},
\code{gene_id}, \code{tx_name}, transcript length metrics, feature geometry columns,
and (if requested) gene symbol/name columns.
}
\description{
Assigns each site to a transcript and region (e.g. fiveUTR/coding/threeUTR/intron),
attaches transcript-level length metrics, computes within-feature coordinates (feature_prop)
and a concatenated metagene coordinate (metagene_prop), and optionally maps gene IDs
to symbols/names via an \code{OrgDb}.
}
\examples{
\dontshow{if (requireNamespace("TxDb.Hsapiens.UCSC.hg38.knownGene", quietly = TRUE) && requireNamespace("GenomicRanges", quietly = TRUE)) withAutoprint(\{ # examplesIf}
txdb <- TxDb.Hsapiens.UCSC.hg38.knownGene::TxDb.Hsapiens.UCSC.hg38.knownGene
gr <- GenomicRanges::GRanges("chr1", IRanges::IRanges(100000, 100000), strand = "+")
S4Vectors::mcols(gr)$label <- 1L
ann <- annotate_sites(gr, txdb = txdb)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link{build_tx_resources}}, \code{\link{add_kmer}},
\code{\link{match_background}}, \code{\link{random_drach_within_transcript}}
}
