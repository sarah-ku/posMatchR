% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02-annotation.R
\name{annotate_sites}
\alias{annotate_sites}
\title{Annotate single-nucleotide sites with transcript context}
\usage{
annotate_sites(
  gr,
  txdb,
  label_col = "label",
  tx_select = c("longest", "first"),
  seqstyle = NULL,
  chrs = NULL,
  resources = NULL,
  drop_unannotated = FALSE,
  orgdb = NULL,
  gene_keytype = NULL,
  gene_symbol_col = "SYMBOL",
  gene_name_col = NULL,
  site_id_col = "site_id",
  preserve_mcols = TRUE,
  overwrite = TRUE,
  cache = NULL,
  quiet = FALSE
)
}
\arguments{
\item{gr}{A single-nucleotide \code{GRanges}. Wider ranges are resized to width 1.}

\item{txdb}{A \code{TxDb}.}

\item{label_col}{Optional label column name to normalise if already present.}

\item{tx_select}{Transcript selection rule for multi-transcript sites: \code{"longest"} or \code{"first"}.}

\item{seqstyle}{Optional sequence naming style, e.g. \code{"UCSC"}.}

\item{chrs}{Optional seqlevels to keep. Use this for species-specific canonical chromosomes.}

\item{resources}{Optional output of \code{\link{build_tx_resources}}.}

\item{drop_unannotated}{If TRUE, drop sites with \code{location == "unannotated"}.}

\item{orgdb}{Optional OrgDb for gene symbol/name mapping.}

\item{gene_keytype}{Keytype for \code{orgdb}; e.g. \code{"ENTREZID"} or \code{"TAIR"}.}

\item{gene_symbol_col}{OrgDb column for symbols.}

\item{gene_name_col}{Optional OrgDb column for names/descriptions.}

\item{site_id_col}{Metadata column used for stable site IDs.}

\item{preserve_mcols}{If FALSE, strip existing metadata before annotation.}

\item{overwrite}{If FALSE, error when existing posMatchR annotation columns are present.}

\item{cache}{Optional cache passed to \code{VariantAnnotation::locateVariants()}.}

\item{quiet}{If TRUE, suppress non-critical progress messages.}
}
\value{
An annotated \code{GRanges}.
}
\description{
Assigns each site to a primary transcript/region using \code{VariantAnnotation::locateVariants()},
attaches transcript-level metrics, computes metagene coordinates and feature distances,
and optionally maps gene IDs to symbols/names via an OrgDb.
}
\details{
Existing user metadata are preserved by default. posMatchR-generated columns are replaced
unless \code{overwrite = FALSE}.
}
