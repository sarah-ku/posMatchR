% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07-universe.R
\name{match_random_background}
\alias{match_random_background}
\title{Randomly match positives to background candidates within groups}
\usage{
match_random_background(
  gr,
  label_col = "label",
  group_col = "gene_id",
  kmer_match = FALSE,
  kmer_col = "kmer",
  location_col = "location",
  locations = NULL,
  seed = 1L,
  return_diagnostics = TRUE
)
}
\arguments{
\item{gr}{Combined and annotated foreground/background \code{GRanges}.}

\item{label_col}{Binary label column, where 1 is foreground and 0 is background.}

\item{group_col}{Metadata column used to constrain matching, e.g. \code{"gene_id"}.}

\item{kmer_match}{If TRUE, match within \code{group_col} and \code{kmer_col}.}

\item{kmer_col}{K-mer column used when \code{kmer_match = TRUE}.}

\item{location_col}{Optional location column.}

\item{locations}{Optional location classes eligible for matching.}

\item{seed}{Random seed.}

\item{return_diagnostics}{If TRUE, attach diagnostics to metadata.}
}
\value{
A \code{GRanges} with the standard posMatchR match columns.
}
\description{
This provides a simple baseline matcher: each positive is matched to one
negative sampled from the same group, usually \code{gene_id} or \code{tx_name}.
Setting \code{kmer_match = TRUE} additionally requires the same k-mer, which
balances motif/k-mer frequencies among successfully matched pairs.
}
