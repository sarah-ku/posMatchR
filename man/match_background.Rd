% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transcript_matching_functions.R
\name{match_background}
\alias{match_background}
\title{Match positives to unique negatives using metagene position and optional bin constraints}
\usage{
match_background(
  gr,
  label_col = "label",
  location_col = "location",
  locations = c("fiveUTR", "coding", "threeUTR"),
  return_diagnostics = FALSE,
  kmer_match = FALSE,
  kmer_col = "kmer",
  meta_col = "metagene_prop",
  meta_tol = 0.05,
  enforce_meta_tol = FALSE,
  meta_k = 200L,
  bin_match = TRUE,
  bin_cols = c("feature_width", "segment_rank", "nexon", "tx_len",
    "dist_from_feature_start", "dist_from_feature_end"),
  n_bins = 5L,
  bin_within = c("location", "global"),
  bin_mode = c("soft", "hard"),
  bin_weight = 0.25,
  log_bin_cols = c("tx_len", "feature_width", "dist_from_feature_start",
    "dist_from_feature_end"),
  seed = 1L
)
}
\arguments{
\item{gr}{A \code{GRanges} containing labels and required annotation columns.}

\item{label_col}{Metadata label column (0/1).}

\item{location_col}{Region column used for matching (default \code{"location"}).}

\item{locations}{Which regions are eligible for matching.}

\item{return_diagnostics}{If TRUE attach a \code{"match_diagnostics"} attribute.}

\item{kmer_match}{If TRUE require exact match on \code{kmer_col}.}

\item{kmer_col}{Column containing k-mers.}

\item{meta_col}{Column containing metagene coordinate (default \code{"metagene_prop"}).}

\item{meta_tol}{Optional tolerance window for metagene closeness.}

\item{enforce_meta_tol}{If TRUE leave positives unmatched when no negative within tolerance exists.}

\item{meta_k}{Local search window size in meta-sorted negatives.}

\item{bin_match}{If TRUE use quantile bins of \code{bin_cols} as a secondary match criterion.}

\item{bin_cols}{Numeric feature columns to bin.}

\item{n_bins}{Number of quantile bins.}

\item{bin_within}{Compute bins globally or within each location.}

\item{bin_mode}{\code{"soft"} uses a penalty; \code{"hard"} requires exact bin equality.}

\item{bin_weight}{Weight of the bin penalty relative to metagene closeness.}

\item{log_bin_cols}{Columns log1p-transformed prior to binning.}

\item{seed}{Random seed (affects tie-breaking order only).}
}
\value{
A \code{GRanges} with standard match columns added.
}
\description{
Creates a background set by pairing each positive site to a unique negative site within
the same region (and optionally the same k-mer), prioritizing closeness in metagene
coordinate and optionally refining matches using quantile-binned feature covariates.
}
\details{
The function writes standard output columns used by downstream plotting helpers:
\code{matched_negative_id}, \code{matched_positive_id}, \code{is_matched_negative},
\code{is_positive}, \code{meta_delta}, and \code{match_set}.
}
\seealso{
\code{\link{subset_matched_pairs}}, \code{\link{plot_metagene_density}}
}
