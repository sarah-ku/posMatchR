% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10-site-enrichment.R
\name{site_enrichment_profile}
\alias{site_enrichment_profile}
\title{Compute occurrence profiles around single-nucleotide sites}
\usage{
site_enrichment_profile(
  gr,
  query,
  genome = NULL,
  query_type = c("auto", "features", "motif"),
  query_name = NULL,
  window = 250L,
  set_col = NULL,
  positive_value = "positive",
  negative_value = "matched_negative",
  include_other = FALSE,
  matched_only = FALSE,
  method = c("auto", "iupac", "universalmotif"),
  scan_rc = FALSE,
  hit_position = c("center", "start", "end"),
  feature_position = c("center", "start", "end"),
  require_same_strand = TRUE,
  weight_col = NULL,
  threshold = 0.8,
  threshold_type = "logodds",
  nthreads = 1L,
  bin_size = 1L,
  smooth_window = 7L,
  seqstyle = NULL,
  chrs = NULL,
  drop_unstranded = TRUE,
  drop_edge_positions = FALSE,
  edge_trim = NULL,
  center_exclude = 0L,
  normalise_per = 1000L,
  window_mode = c("genomic", "transcript", "auto"),
  txdb = NULL,
  resources = NULL,
  tx_name_col = "tx_name",
  tx_pos_col = "tx_pos"
)
}
\arguments{
\item{gr}{A single-nucleotide focal GRanges. For matched-set comparisons, pass
subset_matched_sets(gr) or set matched_only = TRUE.}

\item{query}{Either a point-feature GRanges/list of GRanges objects, or a motif
query. Character motifs are treated as exact/IUPAC RNA/DNA motifs. A
universalmotif object can be supplied when method = "universalmotif".}

\item{genome}{A BSgenome-like object. Required for motif queries; ignored for
supplied GRanges feature queries.}

\item{query_type}{"auto", "features", or "motif". In auto mode, GRanges inputs
are treated as features and character/universalmotif inputs are treated as
motifs.}

\item{query_name}{Optional display name. For multiple motifs or feature sets,
this can be a vector of names.}

\item{window}{Number of bp/nt on each side of the focal site.}

\item{set_col}{Optional grouping column. Defaults to "match_set" if present.}

\item{positive_value}{Value in set_col marking matched positives.}

\item{negative_value}{Value in set_col marking matched negatives.}

\item{include_other}{Include other groups from set_col.}

\item{matched_only}{If TRUE and canonical match columns are present, subset to
reciprocal matched pairs before profiling.}

\item{method}{Motif-scanning method: "auto", "iupac", or "universalmotif".
Ignored for supplied GRanges feature queries.}

\item{scan_rc}{Scan reverse-complement motif instances as well. Usually FALSE
for RNA-oriented analyses when site strands are known.}

\item{hit_position}{For motifs, whether to record the start, end, or centre of
each motif hit.}

\item{feature_position}{For supplied features, whether to record the start,
end, or centre of each feature.}

\item{require_same_strand}{If TRUE, supplied feature queries are counted only
on the same strand as focal sites.}

\item{weight_col}{Optional metadata column in supplied feature GRanges used for
score-weighted profiles.}

\item{threshold}{Threshold passed to universalmotif::scan_sequences().}

\item{threshold_type}{Threshold type passed to universalmotif::scan_sequences().}

\item{nthreads}{Number of threads for universalmotif scanning.}

\item{bin_size}{Position bin size.}

\item{smooth_window}{Moving-average smoothing width in bins. Defaults to 7.}

\item{seqstyle}{Optional seqlevel style to apply.}

\item{chrs}{Optional seqlevels to keep. Usually not needed if inputs are already harmonised.}

\item{drop_unstranded}{If TRUE, omit focal sites on '*' strand.}

\item{drop_edge_positions}{Logical or integer. If TRUE, omit edge positions.}

\item{edge_trim}{Number of bp/nt to omit from each edge. Overrides drop_edge_positions.}

\item{center_exclude}{Number of bp/nt around the central focal site to omit.
Defaults to 0.}

\item{normalise_per}{Scale factor for normalised rates. For example, 1000
reports occurrences per 1000 focal sites.}

\item{window_mode}{For motif queries only: "genomic", "transcript", or
"auto". The default genomic mode follows the input GRanges directly.}

\item{txdb}{Optional TxDb for transcript-window motif scanning.}

\item{resources}{Optional build_tx_resources(txdb) output for transcript-window motif scanning.}

\item{tx_name_col}{Transcript-name metadata column for transcript-window motif scanning.}

\item{tx_pos_col}{Transcript-position metadata column for transcript-window motif scanning.}
}
\value{
A data.frame with relative position, group, feature/query name, counts,
site counts, and normalised rates.
}
\description{
This is the main enrichment-profile interface in posMatchR. The query can be
either a supplied point-feature GRanges object, such as iCLIP crosslink sites,
or an RNA/DNA motif such as "UNUNU", "YUGUM", or "DRACH". Motif queries are
scanned internally in strand-oriented windows around the focal sites and are
treated as motif-hit occurrences. Feature queries are counted by overlap with
strand-oriented windows around the focal sites.
}
\details{
Character motifs are interpreted as exact/IUPAC RNA or DNA patterns. RNA U is
converted internally to DNA T before scanning the genome sequence. Relative
positions are reported in RNA orientation: for minus-strand focal sites,
upstream/downstream positions are flipped so that +10 means 10 nt downstream in
the transcript direction.
}
