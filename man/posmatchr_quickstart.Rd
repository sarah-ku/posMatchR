% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05-quickstart.R
\name{posmatchr_quickstart}
\alias{posmatchr_quickstart}
\title{Quickstart: annotate sites and optionally build a matched background}
\usage{
posmatchr_quickstart(
  sites,
  txdb,
  background = NULL,
  genome = NULL,
  orgdb = NULL,
  seqstyle = NULL,
  chrs = NULL,
  tx_select = "longest",
  label_col = "label",
  site_id_col = "site_id",
  k = 5L,
  match = !is.null(background),
  match_args = list(),
  resources = NULL,
  drop_unannotated = FALSE,
  make_plots = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{sites}{Positive/input sites as a \code{GRanges}.}

\item{txdb}{A \code{TxDb}.}

\item{background}{Optional candidate background \code{GRanges}.}

\item{genome}{Optional genome object for k-mer extraction.}

\item{orgdb}{Optional OrgDb for gene symbols.}

\item{seqstyle}{Optional sequence naming style.}

\item{chrs}{Optional seqlevels to keep.}

\item{tx_select}{Transcript selection rule.}

\item{label_col}{Label column.}

\item{site_id_col}{Site ID column.}

\item{k}{K-mer width when \code{genome} is supplied. Set NULL to skip.}

\item{match}{If TRUE, run \code{\link{match_background}}. Defaults to TRUE when
\code{background} is supplied.}

\item{match_args}{Named list of additional arguments passed to \code{\link{match_background}}.}

\item{resources}{Optional precomputed transcript resources.}

\item{drop_unannotated}{If TRUE, drop unannotated rows.}

\item{make_plots}{If TRUE, include default ggplot objects in the returned list.}

\item{quiet}{If TRUE, suppress messages.}
}
\value{
A list with \code{gr}, \code{table}, \code{matched_pairs}, \code{diagnostics}, and optionally \code{plots}.
}
\description{
This is the simplest entry point for a vanilla single-nucleotide \code{GRanges}.
With only \code{sites} and \code{txdb}, it returns annotated sites. If
\code{background} is supplied, positives and background candidates are combined,
annotated, and matched.
}
