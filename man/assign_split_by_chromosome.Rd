% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transcript_matching_functions.R
\name{assign_split_by_chromosome}
\alias{assign_split_by_chromosome}
\title{Assign train/test/validation split by chromosome}
\usage{
assign_split_by_chromosome(
  gr,
  split_col = "split",
  test_chrs = "chr9",
  val_chrs = "chr20",
  train_label = "training",
  test_label = "testing",
  val_label = "validation",
  normalize_chr_prefix = TRUE,
  warn_missing = TRUE,
  overwrite = TRUE
)
}
\arguments{
\item{gr}{GRanges.}

\item{split_col}{Name of the output metadata column.}

\item{test_chrs}{Character vector of chromosomes to assign as testing.}

\item{val_chrs}{Character vector of chromosomes to assign as validation.}

\item{train_label}{Label used for training rows.}

\item{test_label}{Label used for testing rows.}

\item{val_label}{Label used for validation rows.}

\item{normalize_chr_prefix}{If TRUE, attempt to match "chr" prefix style.}

\item{warn_missing}{If TRUE, warn if any requested chromosomes are not present.}

\item{overwrite}{If FALSE and split_col already exists, error.}
}
\value{
GRanges with an added metadata column split_col (factor).
}
\description{
Adds a metadata column to a GRanges object indicating whether each site belongs
to the training, testing, or validation set based on chromosome.
}
\details{
Default behavior:
\itemize{
\item chr20 -> validation
\item chr9  -> testing
\item everything else -> training
}

The function attempts to harmonize "chr" prefix style (e.g. "9" vs "chr9")
between user-supplied chromosomes and seqnames(gr).
}
