% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07-universe.R
\name{make_kmer_universe}
\alias{make_kmer_universe}
\title{Build a foreground k-mer background universe}
\usage{
make_kmer_universe(
  foreground,
  txdb,
  genome,
  kmers = NULL,
  kmer_col = "kmer",
  min_count = 1L,
  site_offset = NULL,
  scope = c("genes", "transcripts", "all"),
  regions = c("fiveUTR", "coding", "threeUTR"),
  resources = NULL,
  gene_id_col = "gene_id",
  tx_name_col = "tx_name",
  exclude_foreground = TRUE
)
}
\arguments{
\item{foreground}{Annotated foreground \code{GRanges} containing \code{kmer_col}.}

\item{txdb}{A \code{TxDb} with seqlevels compatible with \code{genome}.}

\item{genome}{A BSgenome-like object accepted by \code{Biostrings::getSeq()}.}

\item{kmers}{Optional explicit k-mer vector. If NULL, k-mers are taken from \code{foreground}.}

\item{kmer_col}{Metadata column containing observed k-mers.}

\item{min_count}{Drop foreground k-mers observed fewer than this many times.}

\item{site_offset}{One-based position in the k-mer to use as the point site. NULL uses the centre.}

\item{scope}{Use all transcripts in foreground genes, foreground transcripts,
or all TxDb transcripts.}

\item{regions}{Transcript regions to scan.}

\item{resources}{Optional \code{build_tx_resources(txdb)} output.}

\item{gene_id_col}{Foreground gene ID column.}

\item{tx_name_col}{Foreground transcript-name column.}

\item{exclude_foreground}{If TRUE, remove coordinates already present in foreground.}
}
\value{
Candidate background sites as \code{GRanges}.
}
\description{
Finds occurrences of observed k-mers, usually the site-centred k-mers already
added by \code{add_kmer()}, within the selected transcript space. Matching later
with \code{kmer_match = TRUE} gives an exact foreground/background k-mer balance
for successfully matched pairs.
}
