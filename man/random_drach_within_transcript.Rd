% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transcript_matching_functions.R
\name{random_drach_within_transcript}
\alias{random_drach_within_transcript}
\title{Sample a random DRACH background site within the same transcript}
\usage{
random_drach_within_transcript(
  gr,
  label_col = "label",
  tx_col = "tx_name",
  location_col = "location",
  locations = c("fiveUTR", "coding", "threeUTR"),
  kmer_col = "kmer",
  match_location = TRUE,
  match_kmer = FALSE,
  meta_col = "metagene_prop",
  seed = 1L,
  prefix = "txrand",
  write_standard_cols = TRUE,
  overwrite_standard_cols = TRUE,
  assert_invariants = TRUE,
  return_diagnostics = TRUE
)
}
\arguments{
\item{gr}{A \code{GRanges} containing annotation columns from \code{\link{annotate_sites}}
and sequence context from \code{\link{add_kmer}}.}

\item{label_col}{Metadata label column (0/1).}

\item{tx_col}{Transcript identifier column (default \code{"tx_name"}).}

\item{location_col}{Region column (default \code{"location"}).}

\item{locations}{Allowed regions for matching.}

\item{kmer_col}{Column containing k-mers (default \code{"kmer"}).}

\item{match_location}{If TRUE require the negative to be in the same region as the positive.}

\item{match_kmer}{If TRUE require exact k-mer match.}

\item{meta_col}{Optional metagene coordinate column used to compute \code{meta_delta}.}

\item{seed}{Random seed controlling sampling within transcript strata.}

\item{prefix}{Prefix for function-specific output columns.}

\item{write_standard_cols}{If TRUE write canonical columns used by plotting helpers.}

\item{overwrite_standard_cols}{If FALSE, do not overwrite canonical columns if they already exist.}

\item{assert_invariants}{If TRUE perform internal consistency checks.}

\item{return_diagnostics}{If TRUE attach a \code{<prefix>_diagnostics} attribute.}
}
\value{
A \code{GRanges} with prefix match columns (and optionally canonical match columns).
}
\description{
For each positive site, attempts to select a unique negative DRACH site from the same
transcript, optionally matching location and/or exact k-mer. This yields a transcript-local
background set that can be compared to \code{\link{match_background}}.
}
\details{
Optionally writes the same standard match columns produced by \code{\link{match_background}}
so downstream plotting functions work without modification.
}
\seealso{
\code{\link{match_background}}, \code{\link{subset_matched_pairs}}
}
