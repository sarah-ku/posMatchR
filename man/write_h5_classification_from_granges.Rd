% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transcript_matching_functions.R
\name{write_h5_classification_from_granges}
\alias{write_h5_classification_from_granges}
\title{Write classification-ready HDF5 splits (train/val/test) from a single GRanges}
\usage{
write_h5_classification_from_granges(
  gr,
  bsgenome,
  out_dir = "bpnet_data",
  prefix = "dataset_cls",
  flank = 500L,
  label_col = "label",
  split_col = "split",
  id_col = "id",
  chunk_n = 10000L,
  compression_level = 6L,
  overwrite = TRUE,
  balance_train = FALSE,
  seed = 1337L,
  metadata_cols = NULL,
  drop_unknown_splits = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{gr}{A GRanges containing both classes (label 0/1) and a split column.}

\item{bsgenome}{A BSgenome object (or any object supported by Biostrings::getSeq()).}

\item{out_dir}{Output directory.}

\item{prefix}{Subdirectory name inside out_dir where files will be written.}

\item{flank}{Integer flank size; window width is 2*flank + 1 (default 500).}

\item{label_col}{Metadata column containing 0/1 class labels (default "label").}

\item{split_col}{Metadata column containing split labels (default "split").}

\item{id_col}{Metadata column containing unique IDs (default "id"). If missing, one is created.}

\item{chunk_n}{Chunk size for HDF5 writing (rows per write block).}

\item{compression_level}{gzip compression level for X_int (0-9; default 6).}

\item{overwrite}{If TRUE, overwrite existing .h5 files.}

\item{balance_train}{If TRUE, downsample the \emph{training} split to balance labels 0/1.}

\item{seed}{Random seed (used for balancing only).}

\item{drop_unknown_splits}{If TRUE, rows with split labels not mappable to train/val/test are dropped.}

\item{quiet}{If TRUE, suppress messages.}

\item{metadata_col}{metadata columns (from mcols(gr)) to write to CSV.
If NULL, a sensible default set is attempted (only columns that exist are used).}
}
\value{
Invisibly returns the output directory path (file.path(out_dir, prefix)).
}
\description{
This function expects a GRanges that already contains both positives and negatives
(e.g. after subsetting to matched pairs) and already contains a split column
indicating train/val/test membership.
}
\details{
For each site, it extracts a centered sequence window (2*flank+1 bp) from the
reference genome, pads out-of-bounds sequence with 'N', orients sequences by strand
(reverse-complement for '-' strand), and encodes bases as integers:
A=0, C=1, G=2, T=3, N=4. The encoded matrix is written as dataset "X_int", and labels
as dataset "y" in HDF5 files {train,val,test}.h5.
}
